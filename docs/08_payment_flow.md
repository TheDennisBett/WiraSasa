# Payment Flow

## Business Rule
Payments happen outside the app through MPESA. The app stores invoice and receipt history rather than processing card rails directly.

## Planned Flow
1. Provider completes job
2. Provider generates invoice
3. Client pays via MPESA outside the app
4. Provider marks payment as received
5. Receipt appears in activity history

## Phase 1
Only the navigation and UI placeholders are present.
